1. Stone Stephenson, Jacob Marshall

2. You will be prompted two options upon running the program. entering 1 will prompt you to enter value k and display Fib(k) and GCD(Fib(k+1), Fib(k)). Then you will be prompted for task 2 to enter values a and n for a^n

3. Task 1 recursively finds the fibanacci sequence and uses it to recursively
    find the worst case efficiency in GCD. amount of additions and multiplicatioins
    are counted
    Task 2 recursively computes a^n using 3 different algorithms, a scatterplot is produced showing the number of multiplications